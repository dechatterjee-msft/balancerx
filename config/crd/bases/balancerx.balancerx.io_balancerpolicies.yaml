---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: balancerpolicies.balancerx.balancerx.io
spec:
  group: balancerx.balancerx.io
  names:
    kind: BalancerPolicy
    listKind: BalancerPolicyList
    plural: balancerpolicies
    singular: balancerpolicy
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BalancerPolicy is the Schema for the balancerpolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              BalancerPolicySpec defines the desired state of BalancerPolicy
              BalancerPolicySpec is the desired state.
            properties:
              balancer:
                description: Balancer strategy (ringhash|roundrobin|leastconn)
                enum:
                - ringhash
                - roundrobin
                - leastconn
                type: string
              gvr:
                description: 'GVR string: group/version/resource'
                pattern: ^[^/]+/[^/]+/[^/]+$
                type: string
              sourceNamespace:
                minLength: 1
                type: string
              workerSelector:
                description: Label selector for worker namespaces
                minLength: 1
                type: string
            required:
            - gvr
            - sourceNamespace
            - workerSelector
            type: object
          status:
            description: BalancerPolicyStatus defines the observed state of BalancerPolicy
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
